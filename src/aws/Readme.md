# AWS Notes

This folder is currently a placeholder for cloud-related documentation and architecture references.

## Current status

This repository does not contain a full AWS deployment package, Lambda source code, Terraform templates, CloudFormation files, or SNS implementation code.

## What is visible in the repository

- The frontend JavaScript references a Lambda endpoint URL.
- The frontend also includes a Vercel rewrite configuration.
- The project documentation discusses a cloud-based deployment concept.

## Important note

The AWS-related elements are not fully implemented in the checked-in code. They should be treated as proposed or externally hosted functionality, not as a complete repository-backed AWS deployment.

